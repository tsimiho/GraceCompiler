fun y(ref e : char; ref j, c : char[16]; m, n : int[]; j : char[]; b : int[][26]) : int
fun j(ref c, bar : int[][6]; main : int[]; main, main, e : int[][5]) : int;
var f : int;
fun d(foo, boo : char; ref i, c, c : char[]) : nothing;

{
  while 11 >= '\'' do ;
  d <- d(z, g);
}
