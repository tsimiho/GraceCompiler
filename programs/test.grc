fun hello () : nothing

    var NumberOfRings : char[20];

    fun test (ref t: char[]): nothing

    {
        writeChar(t[1]);
    }

{
    readString(20, NumberOfRings);
    test(NumberOfRings);
}
